[pytest]
asyncio_mode = auto
pythonpath = .
testpaths =
    tests/core
    tests/bypass
    tests/network
    tests/api_worker
    tests/attack
markers =
    integration: marks tests requiring external/live server or network integration
addopts = --import-mode=importlib
